// package Notes-learning;

// public class syntax {
    
//     static {
        // static initializer
        // uses UIManager.put(key, value) to globally override how specific components (tooltips, combo boxes, popup menus) are rendered, ensuring they have a consistent dark, modern look. 

        // UIManager.put(...): This method updates the default UI properties for the entire application.

        // ToolTip: Sets the tooltip background to a dark gray (50, 50, 50) and the text color to white. 

        // ComboBox: Styles JComboBox components. It sets a dark gray background (40, 40, 40), white text, and a distinct blue (70, 100, 180) for the background of selected items. 

        // PopupMenu.border: Gives dropdown menus (like combo box dropdowns or context menus) a subtle, light-gray border (70, 70, 70) to distinguish them from the dark background. 
//     }
// }
